{
  "name": "charted-sails",
  "description": "Libraries and tools for the marine world!",
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "build": "lerna run build",
    "test": "yarn run jest",
    "deploy": "cd logbook-viewer && yarn deploy"
  },
  "workspaces": [
    "*"
  ],
  "devDependencies": {
    "@types/node": "^10.3.3",
    "lerna": "^2.11.0",
    "typescript": "^2.9.2"
  }
}
